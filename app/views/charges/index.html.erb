<h1>La liste des invités : </h1>


</br>
<p><%= link_to 'Éditer l\'évènement', edit_event_path(@event.id), class: 'btn btn-outline-secondary' %></p>
</br>

<table class="table">
  <thead>
    <tr>
      <th scope="col">id</th>
      <th scope="col">Prénom</th>
      <th scope="col">Nom</th>
      <th scope="col">Email</th>
    </tr>
  </thead>
  <tbody>
<% @attendance.each do |f|  %>
    <tr> 
      <td><%= f.user_id %></td>
      
      <%# <td> f.first_name </td><td> f.last_name</td><td> f.email </td> %>
    </tr> 
<% end %>
</tbody>
</table>